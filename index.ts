// index.ts
// ---
// placeholder entry point.
// this file is not used at runtime — each step in `steps/` is compiled
// into its own standalone binary via `bun run build`.
// see build.ts for the compilation logic and workflow.example.yaml
// for usage examples.
// ---

console.log("nothing to run — use `bun run build` to compile step binaries.");
console.log("see workflow.example.yaml for usage in github actions.");
